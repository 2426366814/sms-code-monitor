## 实现"不使用任何模拟数据"规则

### 1. 智能体设置

**检查并修改 `.trae-agent.json` 文件**
- 验证文件是否存在
- 如果存在，添加 `"no_mock_data": true` 配置项
- 如果不存在，创建包含该配置项的文件

### 2. 开发文档更新

**更新 `DEVELOPMENT.md` 文件**
- 在智能体设置部分（11.1 主要设置）添加 `no_mock_data: true` 配置项
- 明确说明该配置项的作用："禁止使用任何模拟数据，所有功能必须直接调用实际API请求"

**增强 `多用户动态验证码监控系统开发文档.md`**
- 在智能体优化配置部分（3.3）添加 `no_mock_data: true` 配置说明
- 在开发规范部分（8.1）进一步强调禁止使用模拟数据的要求
- 在测试验证部分添加"确保不使用任何模拟数据"的检查项

### 3. 验证修改

**确保所有修改生效**
- 检查 `.trae-agent.json` 文件是否正确配置
- 验证开发文档中的修改是否完整
- 确保所有相关部分都明确了禁止使用模拟数据的规则

### 4. 执行步骤

1. 检查并修改 `.trae-agent.json` 文件
2. 更新 `DEVELOPMENT.md` 中的智能体设置
3. 增强 `多用户动态验证码监控系统开发文档.md` 中的相关内容
4. 验证所有修改
5. 重新打包项目（如果需要）

### 5. 预期结果

- 智能体设置中明确禁止使用任何模拟数据
- 开发文档中详细说明了该规则的要求和意义
- 所有开发人员和智能体都能遵守该规则

### 6. 风险评估

- 如果 `.trae-agent.json` 文件不存在，创建新文件可能需要额外的配置
- 需要确保所有相关文档都更新到位，避免遗漏

### 7. 后续维护

- 定期检查智能体配置和开发文档，确保规则持续有效
- 新开发的功能必须严格遵守该规则
- 代码审查时应检查是否使用了模拟数据