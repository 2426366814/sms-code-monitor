## 任务计划

1. **移除模拟数据生成逻辑**：删除所有模拟数据生成代码，直接使用实际API请求
2. **修复退出功能**：确保用户和管理员退出功能正常工作
3. **修复批量添加监控项功能**：确保API请求正确发送，并处理各种响应情况
4. **优化错误处理**：提供清晰的错误信息，帮助用户了解问题

## 具体步骤

### 1. 移除模拟数据生成逻辑
- 删除 `generateMockApiKey` 函数
- 修改API密钥获取逻辑，直接使用实际API请求，不使用模拟数据
- 移除所有相关的模拟数据生成代码

### 2. 修复退出功能
- 检查所有页面中的 `logout` 函数实现
- 确保退出函数正确清除本地存储的token和用户信息
- 确保退出后正确跳转至登录页面

### 3. 修复批量添加监控项功能
- 修改 `handleBulkAddMonitorSubmit` 函数，优化API请求处理
- 处理不同的响应结构
- 添加详细的错误信息
- 确保表单正确重置

### 4. 优化错误处理
- 提供更详细的错误信息，包含HTTP状态码和具体错误
- 优化用户提示，让用户了解具体问题
- 添加日志记录，便于调试

## 预期效果

- 移除所有模拟数据，完全使用实际API
- 退出功能恢复正常
- 批量添加监控项功能正常工作
- 提供清晰的错误信息

请批准执行以上修复计划。