## 任务目标

修复Nginx配置使后端API能正确访问，并测试完整的用户注册、登录、管理员功能。

## 当前问题

1. Nginx配置中的 `location /backend/` 与 PHP location 存在冲突
2. 请求 `/backend/api/auth/` 返回404，但 `/backend/test.php` 可以正常访问
3. auth API文件中的 `new Database()` 需要改为 `Database::getInstance()`

## 执行计划

### 第一阶段：修复Nginx配置

1. 修改Nginx配置文件，正确配置 `/backend/` location
2. 确保PHP文件能正确处理PATH\_INFO
3. 重载Nginx配置

### 第二阶段：修复API文件

1. 修复 auth/index.php 中的数据库连接问题
2. 检查并修复其他API文件中的类似问题
3. 重启PHP-FPM清除缓存

### 第三阶段：测试用户功能

1. 测试用户注册功能
2. 测试用户登录功能
3. 测试用户修改密码功能
4. 测试用户修改用户名功能

### 第四阶段：测试管理员功能

1. 测试管理员登录
2. 测试管理员查看用户列表
3. 测试管理员添加用户
4. 测试管理员修改用户信息
5. 测试管理员删除用户
6. 测试管理员修改用户密码

## 预期结果

* 所有API接口能正常访问

* 用户注册、登录、修改信息功能正常

* 管理员能正常管理用户（增删改查、修改密码）

