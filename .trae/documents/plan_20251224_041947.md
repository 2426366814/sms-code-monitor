## 任务计划

1. **修改批量添加监控数据解析逻辑**：更新 `parseBulkMonitorsData` 函数，支持字段不全时应用默认值
2. **实现平台名自动生成**：使用日期+当天批次的格式生成默认平台名
3. **设置默认状态**：当状态字段缺失时，默认设置为活跃
4. **优化手机号验证**：支持带+号的手机号格式

## 具体步骤

### 1. 修改 `parseBulkMonitorsData` 函数
- 移除严格的4字段要求，支持1-4个字段的输入
- 为缺失字段应用默认值
- 优化错误处理，提供更友好的提示

### 2. 实现平台名自动生成
- 使用当前日期（YYYYMMDD）+ 批次号（3位顺序号）的格式
- 例如：20251224_001, 20251224_002
- 确保同一批次内的平台名唯一

### 3. 设置默认状态
- 当状态字段缺失时，自动设置为 'active'（活跃）
- 保持与现有代码的兼容性

### 4. 优化手机号验证
- 支持带+号的手机号格式（如+13800138000）
- 在验证前自动移除前导+号
- 确保验证逻辑更灵活

## 预期效果

- 用户可以输入不同数量的字段，系统自动应用默认值
- 平台名自动生成，无需手动输入
- 状态默认为活跃，简化操作
- 支持带+号的手机号格式
- 提供清晰的错误提示

请批准执行以上修复计划。