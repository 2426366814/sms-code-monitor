## 测试和修复计划

### 当前发现的问题

1. **数据库配置**：已配置为连接 192.168.7.178 的 MySQL 数据库（数据库名：jm，用户名：jm，密码：ck123456@）

2. **登录功能问题**：
   - 登录页面只有一个统一的登录表单
   - 后端API支持通过用户名或邮箱登录
   - 但前端只发送了 `email` 字段，没有处理管理员和普通用户的区分
   - 数据库中只有一个默认管理员用户（admin），没有普通用户

3. **数据库初始化问题**：
   - SQL文件中有创建管理员用户的语句，但密码是占位符
   - 缺少普通用户数据
   - 需要检查表结构是否正确创建

### 修复步骤

1. **测试数据库连接**
   - 验证数据库是否可连接
   - 检查表结构是否正确

2. **修复登录功能**
   - 修改登录页面，支持用户名/邮箱登录
   - 确保后端正确处理登录请求
   - 添加默认普通用户用于测试

3. **测试所有功能**
   - 用户登录/注册
   - 管理员登录
   - 监控项管理
   - 分组管理
   - 验证码复制和弹窗提醒

4. **修复发现的问题**
   - 根据测试结果修复bug
   - 确保前后端数据交互正常

请确认后开始执行测试和修复工作。